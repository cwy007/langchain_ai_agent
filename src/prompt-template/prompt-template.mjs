import 'dotenv/config';
import {
  ChatOpenAI
} from '@langchain/openai';
import {
  PromptTemplate
} from '@langchain/core/prompts';

// 初始化模型
const model = new ChatOpenAI({
  modelName: process.env.MODEL_NAME,
  apiKey: process.env.OPENAI_API_KEY,
  temperature: 0,
  configuration: {
    baseURL: process.env.OPENAI_BASE_URL,
  },
});

const naiveTemplate = PromptTemplate.fromTemplate(`
你是一名严谨但不失人情味的工程团队负责人，需要根据本周数据写一份周报。

公司名称：{company_name}
部门名称：{team_name}
直接汇报对象：{manager_name}
本周时间范围：{week_range}

本周团队核心目标：
{team_goal}

本周开发数据（Git 提交 / Jira 任务）：
{dev_activities}

请根据以上信息生成一份【Markdown 周报】，要求：
- 有简短的整体 summary（两三句话）
- 有按模块/项目拆分的小结
- 用一个 Markdown 表格列出关键指标（字段示例：模块 / 亮点 / 风险 / 下周计划）
- 语气专业但有一点人情味，适合作为给老板和团队抄送的周报。
`);

const prompt = await naiveTemplate.format({
  company_name: '星航科技',
  team_name: '数据智能平台组',
  manager_name: '刘总',
  week_range: '2025-03-10 ~ 2025-03-16',
  team_goal: '完成用户画像服务的灰度上线，并验证核心指标是否达标。',
  dev_activities: '- 阿兵：完成用户画像服务的 Canary 发布与回滚脚本优化，提交 27 次，相关任务：DATA-321 / DATA-335\n' +
    '- 小李：接入埋点数据，打通埋点 → Kafka → DWD → 画像服务的全链路，提交 22 次\n' +
    '- 小赵：完善画像服务的告警与Dashboard，新增 8 个告警规则，提交 15 次\n' +
    '- 小周：配合产品输出 A/B 实验报表，支持 3 条对外汇报用数据',
});

console.log('格式化后的提示词:');
console.log(prompt);

const stream = await model.stream(prompt);
console.log('\nAI 回答:');
for await (const chunk of stream) {
  process.stdout.write(chunk.content);
}

// 格式化后的提示词:

// 你是一名严谨但不失人情味的工程团队负责人，需要根据本周数据写一份周报。

// 公司名称：星航科技
// 部门名称：数据智能平台组
// 直接汇报对象：刘总
// 本周时间范围：2025-03-10 ~ 2025-03-16

// 本周团队核心目标：
// 完成用户画像服务的灰度上线，并验证核心指标是否达标。

// 本周开发数据（Git 提交 / Jira 任务）：
// - 阿兵：完成用户画像服务的 Canary 发布与回滚脚本优化，提交 27 次，相关任务：DATA-321 / DATA-335
// - 小李：接入埋点数据，打通埋点 → Kafka → DWD → 画像服务的全链路，提交 22 次
// - 小赵：完善画像服务的告警与Dashboard，新增 8 个告警规则，提交 15 次
// - 小周：配合产品输出 A/B 实验报表，支持 3 条对外汇报用数据

// 请根据以上信息生成一份【Markdown 周报】，要求：
// - 有简短的整体 summary（两三句话）
// - 有按模块/项目拆分的小结
// - 用一个 Markdown 表格列出关键指标（字段示例：模块 / 亮点 / 风险 / 下周计划）
// - 语气专业但有一点人情味，适合作为给老板和团队抄送的周报。


// AI 回答:
// ```markdown
// # 数据智能平台组周报
// **📅 2025.03.10 – 2025.03.16 | 星航科技 · 数据智能平台组**
// **汇报人：数据智能平台组 | 直接汇报对象：刘总**

// ---

// ## 📌 本周概要
// 本周团队聚焦用户画像服务的灰度上线，顺利完成 Canary 发布、全链路数据打通和监控告警体系建设，并同步输出了支撑业务决策的 A/B 实验报表。画像服务灰度阶段核心指标均符合预期，整体推进节奏稳健，团队协作紧密，展现了很高的执行力和工程素养。

// ---

// ## 🧩 模块/项目小结

// ### 1. 用户画像服务灰度发布
// - **阿兵**主导完成画像服务的 Canary 发布全流程，并基于灰度实际表现反复优化了发布与回滚脚本，确保异常情况下可快速止损。累计提交 **27 次**，覆盖任务 **DATA-321 / DATA-335**，为灰度稳定运行提供了坚实保障。
// - 灰度阶段各项核心指标（延迟、准确率、吞吐）均在可接受范围，未触发紧急回滚。

// ### 2. 数据链路打通
// - **小李**负责埋点数据接入，打通了 **埋点 → Kafka → DWD → 画像服务** 的全链路，提交 **22 次**。链路打通后，画像服务能够及时消费到清洗后的宽表数据，数据时效性从 T+1 提升至近实时，为后续推荐与营销场景打下基础。

// ### 3. 监控与告警
// - **小赵**针对画像服务新增 **8 个告警规则**，覆盖延迟、错误率、消费积压等关键维度，并完善了 Dashboard 视图，提交 **15 次**。目前告警已接入 On-Call 值班群，可做到 5 分钟内响应。

// ### 4. A/B 实验报表
// - **小周**配合产品团队，基于画像服务灰度数据输出 **3 条对外汇报用数据**，支撑了本周的两次业务汇报，数据口径清晰、解释完备，获得产品侧好评。

// ---

// ## 📊 关键指标看板

// | 模块 | 本周亮点 | 潜在风险 | 下周计划 |
// |------|----------|----------|----------|
// | 画像服务发布 | Canary 发布与回滚脚本打磨成熟，灰度期间零重大故障 | 灰度流量比例提升后，需密切观察延迟与资源水位 | 逐步扩大灰度流量至 30%，完善压力测试方案 |
// | 全链路数据 | 埋点→Kafka→DWD→画像全链路贯通，近实时数据消费已稳定运行 | DWD 清洗逻辑中部分埋点字段映射仍需产品确认 | 推动产品侧确认字段口径，完成链路端到端监控 |
// | 监控告警 | 8 条告警规则上线，Dashboard 覆盖核心黄金指标 | 告警风暴风险尚存，需持续收敛告警阈值 | 根据灰度数据调优阈值，增加告警聚合与静默规则 |
// | A/B 实验报表 | 高效输出 3 条对外汇报数据，获得业务认可 | 目前报表依赖手动取数，频繁需求下效率受限 | 评估自动化报表方案，沉淀常用指标视图 |

// ---

// ## 🙌 结语
// 本周大家在高强度的迭代节奏中依然保持了极佳的交付质量，既完成了画像服务的灰度上线，又为后续的稳定性与自动化埋下了不少“种子”。特别感谢阿兵对发布脚本的精益求精，小李打通全链路的韧劲，小赵给监控体系带来的安全感，以及小周在业务侧关键时刻的给力支撑。下周我们继续稳扎稳打，向更大流量灰度推进。

// > 如有任何问题或建议，欢迎随时沟通。
// ```